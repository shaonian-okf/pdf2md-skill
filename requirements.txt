# Python dependencies for pdf2md
# IMPORTANT: keep this file ASCII-only.
# Non-ASCII comments break "pip install -r" on Windows (pip decodes with the
# system locale codec, e.g. GBK/cp936). See INSTALL.md for details.

# Core PDF processing (text + image extraction)
pymupdf>=1.20

# Image preprocessing and OCR wrapper
# (the tesseract engine itself must be installed separately, see INSTALL.md)
Pillow>=9.0
pytesseract>=0.3.10